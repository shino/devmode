{erl_opts, [debug_info]}.
{deps, []}.

{relx,
 [
  {release, {devmode, git},
   [
    kernel, stdlib
   ]},
  %% works if erts included
  %% {include_erts, true},
  {extended_start_script, true}
 ]}.
